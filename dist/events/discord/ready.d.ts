import { ZorinClient } from '../../structures/ZorinClient';
declare const _default: {
    name: string;
    once: boolean;
    execute(client: ZorinClient): Promise<void>;
};
export default _default;
